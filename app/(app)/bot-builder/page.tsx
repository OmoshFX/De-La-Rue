/**
 * Bot Builder.
 *
 * The iframe that used to live here now lives in the layout, as BotFrame, so it
 * survives navigation between this page and Charts instead of reloading the
 * whole ~47MB bot on every switch. This route just steers it.
 *
 * The mapping lives in BOT_TAB_BY_PATH in components/custom/bot-frame.tsx.
 */
export default function BotBuilderPage() {
  return null;
}
